apiVersion: v1
kind: Service
metadata:
  name: erer
  namespace: namespace-workflow-1686791550213
spec:
  type: LoadBalancer
  selector:
    app: erer
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000